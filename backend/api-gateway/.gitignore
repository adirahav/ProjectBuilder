node_modules
dist
*.log
*.local

# Local env files with real values are ignored at the repo root .gitignore.
# Only .env.example is committed.

# Built frontend assets served by the gateway in production (generated)
public
