# Both variables are REQUIRED for local dev (Vite dev server on :5173) and for
# the Capacitor native build, which is served from a capacitor:// origin and so
# must name an absolute host.
#
# They are OPTIONAL for the single-origin production web deploy: there,
# api-gateway serves the built frontend/dist AND reverse-proxies /api/..., so
# leaving both unset makes every request a relative same-origin path that the
# gateway answers on its own port. See frontend/README.md, "Single-origin
# production deploy".

# Base URL of booking-service. The public booking routes (GET /api/services,
# /api/time-slots, ...) are unauthenticated and are called directly, so the
# frontend talks to booking-service rather than api-gateway for them.
VITE_BOOKING_SERVICE_URL=http://localhost:4001

# Base URL of api-gateway. Every Admin route goes through the gateway, because
# that is where the JWT is verified (PRD F5-F11, AC-7). Admin Login is the one
# unauthenticated route on it — it is how the token is obtained in the first
# place.
VITE_API_GATEWAY_URL=http://localhost:4000
