{
  "name": "reference-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "user-management-service",
    "tour-service",
    "common-service"
  ],
  "scripts": {
    "dev": "concurrently -n common,tour,user -c magenta,blue,green \"npm:dev --prefix common-service\" \"npm:dev --prefix tour-service\" \"npm:dev --prefix user-management-service\"",
    "start": "concurrently -n common,tour,user -c magenta,blue,green \"npm run start --prefix common-service\" \"cross-env PORT=3033 npm run start --prefix tour-service\" \"cross-env PORT=3032 npm run start --prefix user-management-service\""
  },
  "dependencies": {
    "concurrently": "^9.2.4",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
