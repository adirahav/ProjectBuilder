# Each backend service is deployed independently — there is no gateway, so the
# frontend's service layer calls each base URL directly.
VITE_TOUR_SERVICE_BASE_URL=http://localhost:4001
VITE_USER_SERVICE_BASE_URL=http://localhost:4002
